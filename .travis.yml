sudo: required
services:
  - docker

before_install:
        - docker build -t swamisub/chooth-react -f Dockerfile.dev .

script:
  - docker run -e CI=true -t swamisub/chooth-react npm run test -- --coverage
